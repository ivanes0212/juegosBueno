{% extends 'uniJuegosBundle::layout.html.twig' %}


{% block cuerpo -%}
    <center><h1>Categorías</h1></center>

    <table class="records_list">
       
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('categorias_show', { 'id': entity.id }) }}">{{ entity.nombreCat }}</a></td>
                
                
            </tr>
        {% endfor %}
        
        </tbody>
    </table>
<div id="edit">
        <ul>
        <li>
            <a href="{{ path('categorias_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
</div>
    {% endblock %}
